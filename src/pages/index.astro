---
import Layout from '../layouts/layout.astro';
import Card from '../components/card.astro';

const songs = [
  { title: 'Canción 1', artist: 'Artista 1', cover: '/cover1.jpg' },
  { title: 'Canción 2', artist: 'Artista 2', cover: '/cover2.jpg' },
  { title: 'Canción 3', artist: 'Artista 3', cover: '/cover3.jpg' }
];
---

<Layout title="Página Principal">
  <h2>Lista de Canciones</h2>
  <div class="card-container">
    {songs.map(song => (
      <Card title={song.title} artist={song.artist} cover={song.cover} />
    ))}
  </div>
  +

  
  <style>
    .card-container {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
    }
  </style>
</Layout>